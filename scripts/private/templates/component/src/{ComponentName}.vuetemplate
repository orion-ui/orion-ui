<template>
	<pre>{ComponentName}</pre>
</template>

<script setup lang="ts">
import './{ComponentName}.less';
import {ComponentName}Setup from './{ComponentName}Setup';
const props = defineProps({ComponentName}Setup.props);
const setup = new {ComponentName}Setup(props);
defineExpose(setup.publicInstance);
</script>
