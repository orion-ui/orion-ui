import SharedSetup from '../../Shared/SharedSetup';
import SharedProps from '../../Shared/SharedProps';

type Props = SetupProps<typeof {ComponentName}Setup.props>

export default class {ComponentName}Setup extends SharedSetup<Props> {
	static props = {};

	constructor (props: Props) {
		super(props);
	}
}
